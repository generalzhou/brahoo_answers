<div class="tag-title">
<h2>
  <%= @tag.name %>
</h2>
</div>

<ul class="tag-links">
  <% @tag.questions.each do |question| %>
    <li>
      <%= render :partial => 'questions/question_display', :locals => {question: question} %>
    </li>
  <% end %>
</ul>
