<% flash.each do |key, value| %>
  <div class="<%= flash_class(key) %>">
    <%= value %>
  </div>
<% end %>

<%= render "signup_form", :locals => { :user => @user } %>

<h3>Or</h3>
<h3><%= link_to "Return Home", root_path %></h3>
